# BB and Inst number navigation

THis feature only operates on BB and Inst numbers within a single function  
THis also only works for the same function within a single Trace.

BB and Inst numbers are only unique per function, and tracking outside a trace has not been implemented.
